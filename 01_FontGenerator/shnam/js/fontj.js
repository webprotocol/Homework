function familyChange(event) {
    result.style.fontFamily = event.target.value;
}
function fontSize(event) {
    result.style.fontSize = event.target.value + "px"
}
function fontChange(radio) {
    result.style.fontStyle = radio.value;
}
function fontWeight(event) {
    result.style.fontWeight = event.target.value;
}
function fontVariant(radio) {
    result.style.fontVariant = radio.value;
}
function fontStyle(event){
	result.style.font = event.target.value;
}